# This attempts to delete the rainbow scipt from Vim's
# `plugin` directory, so that it is no longer applied to Vim.
# 
# Any changes you've made there will be destroyed! Check your work!
# The script assumes that *this* folder is where you edit `rainbow.vim`.
#
# It is intended for use with Linux/BSD/Unix operating systems.

rm -i -v ~/.vim/plugin/rainbow.vim
